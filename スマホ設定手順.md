# スマホで一問一答アプリを使う手順

このアプリは、**画面に出た問題に声で答えると、合っているかを判定してくれる**ものです。
共通テスト地理の全828問が入っています。

アプリストアからのダウンロードは要りません。**下の手順で、ホーム画面にアイコンを置けます。**

---

## 1. スマホでページを開く

スマホのブラウザで、次のアドレスを開きます。

```
https://narulab-jp.github.io/qa-app/
```

- iPhone なら **Safari**
- Android なら **Chrome**

で開いてください。ほかのブラウザだと、声で答える機能が使えないことがあります。

「一問一答」というタイトルの画面が出れば成功です。

---

## 2. ホーム画面にアイコンを置く

毎回アドレスを入力しなくてよいように、ホーム画面にアイコンを置きます。

### iPhone（Safari）の場合

1. 画面の下にある **共有ボタン**（□から↑が出ている絵）を押す
2. 出てきた一覧を下にスクロールして **「ホーム画面に追加」** を押す
3. 右上の **「追加」** を押す

### Android（Chrome）の場合

1. 画面の右上にある **メニュー**（点が縦に3つ）を押す
2. **「ホーム画面に追加」** を押す
3. **「追加」** を押す

---

## 3. アイコンから始める

ホーム画面に「一問一答」という青いアイコンができています。
**次からはこれを押すだけ**で始められます。

アドレスバーのない、アプリのような画面で開きます。

---

## 4. はじめに自分の名前を登録する

このアプリは、**家族の何人かで使えます。** 記録が混ざらないように、
最初に自分の名前を登録します。

1. 最初に「はじめに、使う人の名前を登録してください。」という画面が出ます
2. 入力欄に **自分の名前**を入れます（「たろう」「長男」など、何でも構いません）
3. **「＋ 新しく追加」** を押します

これで登録は終わりです。次からは、この名前を選ぶだけで始められます。

### 自分の名前が出ているか、毎回たしかめる

登録すると、**画面のいちばん上に「〇〇 として学習中」と出ます。**

```
　┌──────────────────────────┐
　│ たろう として学習中      ［切り替え］ │
　└──────────────────────────┘
```

**ここに出ている名前が自分かどうか、毎回たしかめてください。**
ほかの人の名前が出ていたら、右の **［切り替え］** を押して自分を選びます。

別の人として答えてしまうと、その人の記録に混ざってしまいます。

### 名前を増やす・変える・消す

［切り替え］を押すと、登録した人の一覧が出ます。ここで

- 別の人を選ぶ
- **「＋ 新しく追加」** で人を増やす（何人でも増やせます）
- 名前の右の **「名前」** で名前を変える
- 名前の右の **「削除」** でその人を消す

ができます。

---

## 5. マイクを許可する（最初の1回だけ）

問題の画面で **「🎤 音声で答える」** を押すと、
「マイクへのアクセスを許可しますか」と聞かれます。

**「許可」を押してください。** 声で答えるために必要です。

一度許可すれば、次からは聞かれません。

> 間違えて「許可しない」を押してしまったときは、
> アドレスバーの鍵のマークから設定をやり直せます。
> それでも直らないときは、キーボードで答えられるので、そのまま進めて大丈夫です。

---

## 6. 使い方

### 6-1. ふだんの練習は「間違いノート」から

**トップ画面の一番上にある「間違いノートから出題」**を使うのが基本です。

- 間違えた問題だけが自動でたまっていきます
- **2回続けて正解すると、その問題はノートから消えます**（1回だけでは消えません）
- 「よく間違える順」に出るので、苦手なものから順に練習できます

最初はノートが空です。まずは下の「単元から出題」で何問か解いてみてください。
間違えた問題が自動でノートに入ります。

### 6-2. 単元を選んで解く

「単元を選ぶ」を押すと、第01講から第28講までが並びます。
**いくつでも選べます**（第01〜05講をまとめて、なども可能）。

そのあと、出題数（5問・10問・20問・全部）と、
重要度（S＋Aのみ／すべて）、出題順（シャッフル／もとの順）を選びます。

> **S・A から始めてください。** B は余裕ができてからで構いません。

### 6-3. 全問正解するまで終わらない「周回」

出題のしかたで **「周回する」** を選ぶと、
**選んだ範囲を全部正解するまで終わりません。** 間違えた問題は何度でも出てきます。

途中でやめたいときは、画面の下の **「中断して保存」** を押してください。
続きから再開できます（やり方は7を参照）。

### 6-4. 答え方

- **声で答える** … 「🎤 音声で答える」を押して、答えを声に出す
- **キーボードで答える** … 「キーボードで答える」を押して入力する
- **分からないとき** … 「分からない（スキップ）」を押す

「理由を述べよ」という問題は、**自分で○×を付けます。**
言い方は人それぞれなので、機械では正しく判定できないためです。
模範解答と自分の答えを見比べて、合っていたと思えば○を押してください。

---

## 7. 終わったら必ず「保存」する（大事）

**このアプリは、スマホの中に記録を残しません。**
間違いノートは、**ファイルとして自分で保存する**しくみです。

### 保存のしかた

問題を解き終わると、結果の画面に **「ノートを保存する」** というボタンが出ます。
これを押すと、**自分の名前が入ったファイル**がダウンロードされます。

```
たろう さんの場合 → chiri_note_たろう.json
```

名前が入っているので、家族の分がまざりません。

> **押さずに閉じると、その回の記録は消えます。** 必ず押してください。
> 閉じようとすると「保存していない記録があります」と警告が出ます。

### 次に始めるとき

アプリを開いたら、**まず一番上の「前回のノートを読み込む」を押して、
自分の名前が入ったファイルを選んでください。**（例：`chiri_note_たろう.json`）

**ほかの人のファイルを選ばないように気をつけてください。**
もし違う人のものを選んだときは、

```
このノートは「〇〇」のものです。
今は「△△」として使っています。読み込みますか。
　［〇〇に切り替えて読み込む］　［いいえ（戻る）］
```

と確認が出ます。**自分のものでなければ「いいえ（戻る）」を押してください。**

これで前回までの間違いノートが戻ります。
読み込まずに始めようとすると「ノートなしで始めますか」と確認が出ます。

### 中断した周回を再開するとき

「中断して保存」で保存したファイル（例：`chiri_resume_たろう.json`）を、
トップ画面の **「中断した周回を再開する」** から選ぶと、続きから始められます。
このファイルにも自分の名前が入ります。

---

## 8. 声が使えないとき

次のような場合は、声で答えられません。

- マイクを許可していない
- 電波がない場所（機内モードなど）
- ネットワークの制限がある場所

**そのときは自動でキーボード入力に切り替わります。**
画面にその理由が出るので、そのまま文字で答えてください。
**問題を解くこと自体はできます。**

電波がなくても、一度開いたことがあれば**アプリは起動します**（問題も全部入っています）。
声だけが使えません。

---

## 9. 困ったときは

| こんなとき | どうするか |
|---|---|
| 画面が真っ白 | いったん閉じて、もう一度アイコンから開く |
| 声を認識しない | 静かな場所で、はっきり答える。だめならキーボードで答える |
| ノートが空になった | 「前回のノートを読み込む」で保存したファイルを選ぶ |
| 周回が終わらない | 「中断して保存」を押して、いったんやめる |
| 文字が小さい | トップ画面の「設定」で、文字サイズを「大」にする |
| 別の人の名前が出ている | 画面の上の［切り替え］を押して、自分を選ぶ |
| 記録が別の人と混ざった | 自分のファイルを読み込み直す（名前入りのファイルを選ぶ） |

---

## 10. 間違いノートを紙で持ち歩きたいとき

トップ画面の **「ノートを見る」→「印刷用の表示にする」** を押すと、
印刷向けの画面になります。パソコンから印刷すると、A4の紙にまとまります。

よく間違える順に並び、チェック欄（□□□）が付いています。
